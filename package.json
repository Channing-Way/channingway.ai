{
  "name": "cw-build",
  "version": "0.0.0",
  "private": true,
  "description": "Build-time npm dependencies for the cw-build container image. Tracked by Dependabot's npm ecosystem per R-VERSIONING-1. Not a runtime Node.js application.",
  "dependencies": {
    "wrangler": "4.106.0"
  }
}
